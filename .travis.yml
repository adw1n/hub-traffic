language: java
jdk:
- oraclejdk8
services:
- postgresql

before_install:
- sed -i 's/spring.datasource.password=password//g' src/main/resources/application.properties
# - psql -c 'create database hubtraffic;' -U postgres

install:
- yarn install

script:
- ./mvnw verify
- yarn test

#before_script:

